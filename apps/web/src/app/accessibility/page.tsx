import { UtilityPage } from "@/components/content-hubs";
export default function AccessibilityPage() {
  return (
    <UtilityPage
      description="The build targets keyboard access, visible focus, semantic structure, reduced motion, transcripts, and WCAG-oriented contrast. A production accessibility statement requires final audit and contact details."
      title="Accessibility commitment"
    />
  );
}
